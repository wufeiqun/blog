#coding=utf-8
#题目:给一个先递增再递减的list,用不同方法求最大值,并指明最快的方法.
#假定list中没有重复的值.
#
def  search_big(lst):
	low,high = 0,len(lst) - 1
	while high >= low:
		mid = (low+high)/2
		if lst[mid-1] < lst[mid] < lst[mid+1]:
			low = mid+1
		elif lst[mid-1] > lst[mid] > lst[mid+1]:
			high = mid - 1
		else:
			return mid

if __name__ == '__main__':
	a = [1,2,3,4,5,6,7,11,13,15,19,9,5,1]
	print '列表中最大的数的位置为',search_big(a)
	print '最大的数字为',a[search_big(a)]

#方法二,依次比较,最笨的办法.
#i = 0
#while lst[i] < lst[i+1]:
#	i+=1
#print lst[i]
